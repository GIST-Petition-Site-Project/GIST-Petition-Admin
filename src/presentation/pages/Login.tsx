import LoginForm from '@components/LoginForm';
import { Wrapper } from '@components/common';

const Login = () => {
  return (
    <Wrapper>
      <LoginForm />
    </Wrapper>
  );
};

export default Login;
